# Retrieval Service

Optional standalone retrieval microservice. Core ACL-safe ranking lives in `kip_core.services.retrieval`.

## Status

Scaffold — monolith hosts retrieval until scale requires extraction.
